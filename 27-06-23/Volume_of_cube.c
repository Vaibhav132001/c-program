//volume of cube
#include<stdio.h>
int main()
{
	int length,volume;
	printf("Enter the length:-");
	scanf("%d",&length);
	volume=length*length*length;
	printf("The value of cube is:-%d",volume);
	return 0;
}
